/**
 * Copie para config.js e preencha após implantar o Apps Script.
 * Não commite o token real em repositório público — use um fork privado
 * ou sobrescreva via prompt na primeira visita (salvo no localStorage).
 */
window.MONITOR_CONFIG = {
  /** URL da implantação Web App (termina com /exec) */
  apiUrl: "",
  /** Mesmo valor de API_TOKEN nas propriedades do Apps Script */
  apiToken: "",
};
